@AGENTS.md
"Skill: Extract-UI. Whenever asked to extract a component, create a new file in /components, preserve all Tailwind classes, use a TypeScript interface for any props, export as default, and replace the original code in the parent file with the new import."

"Skill: Verify-Build. After modifying any .tsx file, automatically run npm run lint and npm run build in the terminal context. If errors appear, fix them before stopping."